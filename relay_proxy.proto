syntax = "proto3";

package proto;

option java_multiple_files = true;

option java_package = "com.bigkotik.transparentdatabridge";

service RelayProxy {
  rpc SendChunks(stream FileChunk) returns (FileReceived);
}

message FileChunk {
  bytes chunk = 1;
}

message FileReceived {

}